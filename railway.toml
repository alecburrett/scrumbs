[build]
builder = "nixpacks"

[deploy]
preDeployCommand = "cd packages/db && npx drizzle-kit migrate"
startCommand = "npm run start --workspace=apps/web"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
